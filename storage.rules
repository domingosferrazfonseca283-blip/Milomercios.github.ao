rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    function signedIn() { return request.auth != null; }
    function isAdmin() {
      return signedIn()
        && request.auth.token.email == 'domingosferrazfonseca283@gmail.com'
        && request.auth.token.email_verified == true
        && firestore.get(/databases/(default)/documents/usuarios/$(request.auth.uid)).data.tipo == 'admin'
        && firestore.get(/databases/(default)/documents/usuarios/$(request.auth.uid)).data.email == 'domingosferrazfonseca283@gmail.com';
    }

    match /comprovativos/{uid}/{fileName} {
      allow read: if isAdmin() || (signedIn() && request.auth.uid == uid);
      allow write: if signedIn()
        && request.auth.uid == uid
        && firestore.get(/databases/(default)/documents/usuarios/$(request.auth.uid)).data.tipo == 'vendedor'
        && request.resource.size <= 10 * 1024 * 1024
        && (request.resource.contentType == 'application/pdf' || request.resource.contentType.matches('image/.*'));
      allow delete: if isAdmin() || (signedIn() && request.auth.uid == uid);
    }

    match /{allPaths=**} { allow read, write: if false; }
  }
}
